{
  "name": "nodesnip",
  "version": "2.0.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "vitest run",
    "icons": "node scripts/generate-icons.js",
    "package": "npm run build && rm -f NodeSnip.zip && cd dist && zip -r ../NodeSnip.zip . && echo 'NodeSnip.zip ready'"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "pngjs": "^7.0.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.10"
  }
}
