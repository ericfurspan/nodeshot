{
  "name": "nodeshot",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "vitest run",
    "icons": "node scripts/generate-icons.js",
    "package": "npm run build && cd dist && zip -r ../nodeshot.zip . && echo 'nodeshot.zip ready'"
  },
  "dependencies": {
    "html2canvas": "^1.4.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "vite": "^8.0.14",
    "vitest": "^4.1.7",
    "jsdom": "^24.0.0",
    "pngjs": "^7.0.0"
  }
}
